# 星伽 UI

`shared/xingjia-ui` 是一个“可直接复制到其他项目使用”的通用组件库目录。

## 目录说明

- `src/components/feedback`：反馈类弹层组件（如弹窗、抽屉）
- `src/styles`：共享样式能力（如局部弹层容器）
- `src/index.ts`：统一导出与全局注册插件
- `docs/USAGE.md`：使用教程（中文）
- `docs/ADD_COMPONENT.md`：新增组件教程（中文）

## 当前组件

- `PageModal`（局部容器弹窗，适配微前端场景）
